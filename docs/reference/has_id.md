# Check if data frame has IDs

Check if data frame has IDs

## Usage

``` r
has_id(.data, .id = ".id")
```

## Arguments

- .data:

  A data frame.

- .id:

  Column name to check (default: ".id").

## Value

Logical.
